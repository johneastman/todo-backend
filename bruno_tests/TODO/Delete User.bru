meta {
  name: Delete User
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/users/test
  body: none
  auth: none
}

tests {
  test("Response Code is 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Response body structure is correct", () => {
    const body = res.body;
    expect(body.message).to.equal("Data successfully deleted");
  });
  
}
